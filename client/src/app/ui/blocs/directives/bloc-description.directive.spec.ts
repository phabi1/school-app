import { BlocDescriptionDirective } from './bloc-description.directive';

describe('BlocDescriptionDirective', () => {
  it('should create an instance', () => {
    const directive = new BlocDescriptionDirective();
    expect(directive).toBeTruthy();
  });
});
